#+TITLE: Aula 05 — Estrutura de um Agente (Local)
#+AUTHOR: Guilda de IA — UFVJM 2026.1
#+LANGUAGE: pt-BR
#+PROPERTY: header-args:python :python /home/alchemist/git/guilda-ia/notebooks/.venv/bin/python :session aula05 :results output :async yes

#+STARTUP: overview

Este é o *notebook local* para acompanhar a aula 05 sem depender do Colab.
O modelo roda via Ollama local (porta 11434), usando o endpoint OpenAI-compatible.

Usamos o endpoint *OpenAI-compatible* do Ollama (=/v1/chat/completions=) com
=ChatOpenAI= do =langchain-openai=. Isso funciona com *qualquer* servidor que
fale a API da OpenAI — Ollama, vLLM, TGI, Gemini, OpenAI — bastando trocar
=base_url= e =api_key=.

O =ChatOllama= existe, mas o endpoint OpenAI-compatible é o padrão da
indústria. Aprender assim te prepara pra trocar de provedor mudando uma linha.

* Setup — Criar o venv (rodar uma vez no terminal)

#+BEGIN_SRC text
cd ~/git/guilda-ia/notebooks
uv venv .venv
uv pip install langchain-openai langchain-core requests --python .venv/bin/python
#+END_SRC

* 0. Agente na Mão — =requests= puro

Antes de usar LangChain, vamos construir um agente *do zero* com o que já
sabemos: =requests.post= + dicionários Python.

Assim fica claro o que o LangChain abstrai.

#+BEGIN_SRC python
import requests

LLM_URL = "http://localhost:12434/v1/chat/completions"

def criar_agente(instrucoes, max_historico=20):
    return {
        "instrucoes": instrucoes,
        "historico": [],
        "max_historico": max_historico
    }

def conversar(agente, mensagem):
    messages = [{"role": "system", "content": agente["instrucoes"]}]

    limite = agente["max_historico"]
    for msg in agente["historico"][-limite:]:
        messages.append(msg)

    messages.append({"role": "user", "content": mensagem})

    response = requests.post(
        LLM_URL,
        json={
            "model": "gemma4-e2b:think",
            "messages": messages,
            "stream": False
        }
    )

    resposta = response.json()["choices"][0]["message"]["content"]

    agente["historico"].append({"role": "user", "content": mensagem})
    agente["historico"].append({"role": "assistant", "content": resposta})

    return resposta

print("✅ Agente manual com requests pronto!")
#+END_SRC

#+RESULTS:
: ✅ Agente manual com requests pronto!

#+BEGIN_SRC python
agente = criar_agente("Você é um assistente amigável que responde em português.")

print(conversar(agente, "Oi! Meu nome é Ana."))
print()
print(conversar(agente, "Qual é o meu nome?"))
#+END_SRC

#+RESULTS:
: Olá, Ana! É um prazer te conhecer. 😊
: 
: Como posso te ajudar hoje? Tem alguma dúvida, ou quer conversar sobre algo?
: 
: O seu nome é Ana! 😊

* 1. LangChain + OpenAI-compatible

Montar mensagens na mão funciona, mas é repetitivo. *LangChain* abstrai
esse trabalho.

O endpoint OpenAI-compatible do Ollama (=/v1/chat/completions=) funciona com
=ChatOpenAI= do =langchain-openai=: mesmo modelo local, sem enviar nada pra
nuvem.

#+BEGIN_SRC python
from langchain_openai import ChatOpenAI

llm = ChatOpenAI(
    model="gemma4-e2b:think",
    base_url="http://localhost:12434/v1",
    api_key="nao_precisa",  # Ollama local não usa chave
    temperature=0.7
)

resposta = llm.invoke("O que é LangChain em uma frase?")
print(f"🤖 {resposta.content}")
#+END_SRC

#+RESULTS:
: 🤖 LangChain é um framework que permite aos desenvolvedores construir aplicações complexas e inteligentes, conectando Modelos de Linguagem (LLMs) a fontes de dados e ferramentas externas para realizar tarefas sofisticadas.

* 2. Chat com memória

O LLM não lembra de nada sozinho — cada chamada é independente.
Para ter memória, guardamos o histórico de mensagens e enviamos junto a cada chamada.

Na seção 0, gerenciamos o histórico manualmente com listas de dicts.
No LangChain, podemos fazer o mesmo usando =InMemoryChatMessageHistory= —
que é basicamente uma lista tipada. A lógica é *idêntica*:

1. Guardar mensagens no histórico
2. Passar o histórico para o modelo
3. Salvar a resposta de volta no histórico

#+BEGIN_SRC python
from langchain_core.chat_history import InMemoryChatMessageHistory
from langchain_core.messages import HumanMessage, AIMessage
from langchain_core.prompts import ChatPromptTemplate

# Sessão com histórico (sem gerenciamento de sessões — uma variável simples)
session = InMemoryChatMessageHistory()

# Prompt com placeholder para o histórico
prompt_mem = ChatPromptTemplate.from_messages([
    ("system", "Você é um assistente amigável que responde em português."),
    ("placeholder", "{history}"),
    ("human", "{pergunta}")
])

cadeia_mem = prompt_mem | llm

print("✅ Cadeia com memória pronta!")
#+END_SRC

#+RESULTS:
: ✅ Cadeia com memória pronta!

#+BEGIN_SRC python
# Primeira mensagem — adicionamos ao histórico manualmente
r1 = cadeia_mem.invoke({
    "pergunta": "Meu nome é Ana e eu estudo engenharia.",
    "history": session.messages
})
print(f"🤖 {r1.content}")

session.add_message(HumanMessage(content="Meu nome é Ana e eu estudo engenharia."))
session.add_message(AIMessage(content=r1.content))
#+END_SRC

#+RESULTS:
: 🤖 Olá, Ana! Que ótimo te conhecer. Engenharia é uma área super interessante e desafiadora. 🤩
: 
: Em que posso te ajudar hoje? Seja com estudos, informações, ideias ou qualquer outra coisa, é só me chamar! 😊

#+BEGIN_SRC python
# Segunda mensagem — o modelo lembra do nome!
r2 = cadeia_mem.invoke({
    "pergunta": "Qual é o meu nome e o que eu estudo?",
    "history": session.messages
})
print(f"🤖 {r2.content}")

session.add_message(HumanMessage(content="Qual é o meu nome e o que eu estudo?"))
session.add_message(AIMessage(content=r2.content))
#+END_SRC

#+RESULTS:
: 🤖 Com base no que você me disse, seu nome é **Ana** e você estuda **Engenharia**! 😊

#+BEGIN_SRC python
# Sessão zerada = sem memória
session_nova = InMemoryChatMessageHistory()

r3 = cadeia_mem.invoke({
    "pergunta": "Qual é o meu nome?",
    "history": session_nova.messages  # histórico vazio!
})
print(f"🤖 {r3.content}")
print("👉 Na sessão nova o modelo não sabe nosso nome!")
#+END_SRC

#+RESULTS:
: 🤖 Eu sinto muito, mas eu não sei o seu nome. Você não me disse ainda! 😊
: 
: Se você quiser me dizer, eu ficarei feliz em saber!
: 👉 Na sessão nova o modelo não sabe nosso nome!

* Resumo

- *requests puro* → agente com dict + funções (sem abstração)
- *ChatOpenAI* → conecta LangChain ao LLM via endpoint OpenAI-compatible
- *ChatPromptTemplate* → prompts reutilizáveis com variáveis
- *InMemoryChatMessageHistory* → histórico tipado (mesma lógica da seção 0)
- O padrão =prompt | llm= é a base de tudo no LangChain

Na S06 vamos adicionar *ferramentas* (calculadora, busca) ao agente!
